buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.1.0"
    }
}

// Thư viện hiện được quản lý tập trung trong settings.gradle

task clean(type: Delete) {
    delete rootProject.buildDir
}